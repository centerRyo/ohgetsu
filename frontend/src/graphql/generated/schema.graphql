type Query {
  ingredients: [IngredientType!]!
  genres: [GenreType!]!
}

type IngredientType {
  id: ID!
  name: String!
  category: Category
}

type GenreType {
  id: ID!
  name: String!
}

enum Category {
  MAIN
  SUB
}
